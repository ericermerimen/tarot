// Root layout — locale-specific layout is in [locale]/layout.tsx
// This file exists only as a required Next.js root layout shell.
// The [locale] segment handles all providers, fonts, and metadata.

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
